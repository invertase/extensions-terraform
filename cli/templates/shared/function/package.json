{
  "name": "{{EXTENSION_NAME}}-functions",
  "description": "{{FUNCTION_DESCRIPTION}}",
  "main": "lib/index.js",
  "license": "Apache-2.0",
  "scripts": {
    "gcp-build": "npm run compile",
    "build": "npm run clean && npm run compile",
    "clean": "rimraf lib",
    "compile": "tsc",
    "lint": "biome check .",
    "lint:fix": "biome check --write ."
  },
  "engines": {
    "node": "24"
  },
  "dependencies": {
    "firebase-admin": "^13.7.0",
    "firebase-functions": "^7.0.6"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.5",
    "@types/node": "^24.0.0",
    "rimraf": "^6.1.3",
    "typescript": "^5.9.3"
  },
  "private": true
}
